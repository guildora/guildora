<script setup lang="ts">
defineProps<{
  content: Record<string, unknown>;
  config: Record<string, unknown>;
}>();
</script>

<template>
  <section class="py-16 md:py-24">
    <h2 v-if="content.sectionTitle" class="mb-10 text-center text-3xl font-bold tracking-tight md:text-4xl">
      {{ content.sectionTitle }}
    </h2>
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      <div
        v-for="(member, i) in (content.members as Array<Record<string, unknown>>)"
        :key="i"
        class="flex flex-col items-center text-center"
      >
        <div class="mb-4 h-24 w-24 overflow-hidden rounded-full shadow-md">
          <img v-if="member.avatarUrl" :src="String(member.avatarUrl)" :alt="String(member.name || '')" class="h-full w-full object-cover" />
          <div v-else class="flex h-full w-full items-center justify-center bg-surface-3 text-2xl font-bold text-[var(--color-text-secondary)]">
            {{ String(member.name || '?')[0] }}
          </div>
        </div>
        <p class="text-base font-semibold">{{ member.name }}</p>
        <p v-if="member.role" class="mt-0.5 text-sm text-[var(--color-text-secondary)]">{{ member.role }}</p>
      </div>
    </div>
  </section>
</template>
