import { describe, expect, it } from "vitest";

describe("auth utils", () => {
  it("placeholder - add requireRole tests with h3 mock", () => {
    const roles = ["user", "admin"];
    expect(roles).toContain("admin");
  });
});
